#include "sort.h"

/**
 * radix_sort - sorts an array of integers in ascending order
 * @array: to be sorted
 * @size: size of array
 */

void radix_sort(int *array, size_t size)
{
    if (!array || size < 2)
        return;
}